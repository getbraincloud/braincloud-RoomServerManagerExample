
var config = {
    appId : "",// Fill in the appId
    serverSecret : "",// Fill in the server secret
    serverName : "",// Fill in the server name
    dockerImage : "",// Fill in the docker image to run, along with the tagname
    debug: false
};

module.exports = config;